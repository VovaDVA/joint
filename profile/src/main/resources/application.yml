spring:
  application:
    name: jointProfile

  main:
    allow-bean-definition-overriding: true

  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: postgres
    password: 1234567

  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: update

server:
  port: 8081